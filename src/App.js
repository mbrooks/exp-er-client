import React, { Component } from 'react';
import './App.css';
import ChatContainer from './ChatContainer';

class App extends Component {
  render() {
    return (
      <ChatContainer />
    );
  }
}

export default App;
